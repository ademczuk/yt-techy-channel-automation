# Runtime Files

This directory stores generated content and templates for the `Code Search` automation pipeline.

## Planned layout

- `episodes/<date>/`
  - generated manifest
  - screenshots
  - audio
  - final publish bundle
- `templates/`
  - example config files for sourcing, audio, QA, and delivery

Runtime outputs should stay separate from the renderer source code.
